{
  "name": "kernel-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "E2E demo: Hono server using the @settlegrid/mcp dispatch kernel with sg-balance + x402 payment flows.",
  "scripts": {
    "dev": "npx tsx server.ts",
    "test": "npx tsx client-test.ts",
    "test:e2e": "npx tsx client-test.ts"
  },
  "dependencies": {
    "@settlegrid/mcp": "*",
    "@hono/node-server": "^1.0.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "@types/node": "^22.0.0"
  }
}

# B1.1 — circle-nano ENABLE-GATE SPLIT — SCOPE-CONFIRM TRACE (2026-06-13)

> ① of the ARC (`b1.1-enable-gate-split-handoff-2026-06-12.md` §4.1). Mechanical census +
> LB-1 downstream walk (replace-vs-AND verdict + full misconfig truth table) + DC-15 cited-line
> re-verification + DC-05 forced-test sweep. Every claim below is grounded in a tool result from
> this session (file reads / greps cited inline).

## §0 precondition — re-verified (not assumed)
- `git log --oneline -5`: HEAD `98159e04` (this chunk's handoff-doc commit) atop `5ec73770`
  (the (V) close — "feat(web): (V) pending-row lifecycle …") atop `6465402c`. The (V) CLOSE is
  on `main`. ✓
- `git status --porcelain`: **empty** — tree clean of `apps/web/src/lib/settlement/*` and
  everything else. ✓
- Reference artifacts present: `.audit/v-seal/seal-review.mjs`, `.audit/defect-ledger/INDEX.md`,
  the 2026-05-31 discovery doc. No `.audit/b11-*` dirs yet (expected). ✓

## (a) MECHANICAL CENSUS — every consumer of BOTH gates (tree-wide)
`grep -rn 'isCircleNanoEnabled|isCircleNanoKernelEnabled'` over `*.ts,*.tsx,*.mjs,*.js,*.json,*.md`
(excl. node_modules/.next/dist/coverage) + targeted follow-ups on `CIRCLE_NANO_API_KEY`,
`SETTLEGRID_USDC_RECIPIENT`, `validateCircleNanoPayment`.

### `isCircleNanoEnabled` (the API-key gate) — definition + complete consumer set
| Site | Role | Class |
|---|---|---|
| `lib/circle-nano-proxy.ts:41-43` | **definition**: `return !!process.env.CIRCLE_NANO_API_KEY` | gate def |
| `lib/circle-nano-proxy.ts:50` | `enabled:` arg to the `validateCircleNanoPayment` wrapper (`:45-54`) | **DEAD** (see below) |
| `route.ts:33` | import | import |
| `route.ts:332` | `enabledMap` entry `'circle-nano': isCircleNanoEnabled` (unified dispatch) | **LIVE dispatch** |
| `route.ts:472` | legacy dispatch `if (isCircleNanoEnabled() && isCircleNanoRequest(request))` | **LIVE dispatch** |
| `lib/__tests__/proxy-equivalence.test.ts:75` | import | test |
| `lib/__tests__/proxy-equivalence.test.ts:170` | the **legacy-chain mirror** (re-implements `:472`) | test (lockstep) |
| `proxy/[slug]/__tests__/circle-nano-proxy-settlement.test.ts:87` | `vi.mock` stub `isCircleNanoEnabled: () => true` | test mock |

### `isCircleNanoKernelEnabled` (the recipient gate) — definition + complete consumer set
| Site | Role | Class |
|---|---|---|
| `lib/env.ts:260-264` | **definition**: `return !!getCircleNanoRecipient()` (getter trims `SETTLEGRID_USDC_RECIPIENT`, `:250-258`) | gate def |
| `route.ts:47` | import | import |
| `route.ts:2015` | `handleCircleNanoProxy` **dark-gate (money boundary)** → 503 `CIRCLE_NANO_NOT_CONFIGURED` | FROZEN (funds-safety seal) |
| `circle-nano/settle/route.ts:91` | kernel settle gate → 503 | FROZEN ("already correct") |
| `circle-nano/verify/route.ts:93` | kernel verify gate → `valid:false`@200 | FROZEN ("already correct") |
| `circle-nano/__tests__/e2e-smoke.test.ts:33`, `route.test.ts:57` | kernel-route mocks | test (kernel — out of scope) |
| `proxy/[slug]/__tests__/circle-nano-proxy-settlement.test.ts:203/311` (refs), `:2015` exercised | dark-gate refs | test |

### Non-gate surfaces that NAME circle-nano (LB-2 straggler hunt) — **none consume a gate**
- Static-file census (`*.json/*.yaml/*.yml/*.mdx` for "circle-nano") = **empty** → no static manifest gates on the rail.
- Generated/published surfaces (verified static enums, do NOT read either gate):
  - `.well-known/mpp.json/route.ts:58` — `protocols_supported: ['mcp','mpp',…,'circle-nano','rest']` (literal array).
  - `api/openapi.json/route.ts:120` — `protocol: { enum: […'circle-nano'] }` (schema enum).
  - `learn/protocols/page.tsx` + `learn/protocols/[slug]/page.tsx` — marketing/docs copy (the `[slug]` page already states verification needs "no Circle account or API key").
  - `buildMultiProtocol402` (packages/mcp) — wired **only** into `api/demo/kernel` ("NO REAL MONEY"), a capability teaser (discovery doc §32).
- These describe **product capability**, not **instance enablement** → correctly OUT OF SCOPE (matches the 2026-05-31 discovery doc's full-surface enumeration: "No live surface was missed").

### `CIRCLE_NANO_API_KEY` — read in EXACTLY ONE place
`circle-nano-proxy.ts:42` (inside `isCircleNanoEnabled`). The only other hits are a comment
(`env.ts:248`) and a test stub (`proxy-equivalence.test.ts:266`). **No Circle SDK / API call
anywhere needs the key** — it is a pure legacy dispatch gate.

### `validateCircleNanoPayment` app-side wrapper (holds `:50`) is **DEAD**
`grep validateCircleNanoPayment apps/web --excl tests,circle-nano-proxy.ts` = **empty**. No app
route imports it; the live verification path uses `validateCircleNanoCredentialString`
(`:2029`). The core `packages/mcp` `validateCircleNanoPayment` is a separate (frozen) symbol used
internally by the adapter. → `:50` is an inert export.

**LB-2 (consumer-set completeness, DC-07) — RESOLVED.** The complete LIVE consumer set of the
API-key gate is exactly: `route.ts:332` (unified enabledMap) + `route.ts:472` (legacy dispatch),
coupled by an equivalence contract to the test mirror `proxy-equivalence.test.ts:170`; plus the
DEAD wrapper `circle-nano-proxy.ts:50`. **Zero stragglers** in any manifest / openapi / learn /
status surface. Post-fix consumer count of `isCircleNanoEnabled` (live) = **0**.

## (b) LB-1 DOWNSTREAM WALK — replace-vs-AND + the misconfig truth table
**Question:** does the dispatch path downstream genuinely consume `CIRCLE_NANO_API_KEY`?

**Walk of the dispatch target `handleCircleNanoProxy` (`route.ts:1997-2130`), read in full:**
1. `:2003` tool lookup → `:2007` cost.
2. `:2015` **dark-gate** `if (!isCircleNanoKernelEnabled())` → 503. Comment `:2014`: *"closes the
   phantom-credit hole **regardless of the dispatch enable-gate**"* — the money boundary is
   deliberately independent of dispatch.
3. `:2029` `validateCircleNanoCredentialString(header, …)` — the **authoritative** verifier.
   Reads only `getCircleNanoRecipient()`; its own comment (`circle-nano-proxy.ts:65`): *"Enabled
   by the recipient env alone — no Circle account or API key required."*
4. `:2037` 402 via `generateCircleNano402Response` (omits discovery when recipient invalid/unset,
   `circle-nano-proxy.ts:170-172`) — reached ONLY past the `:2015` recipient gate.
5. `:2084` `executeCircleNanoSettlement` (gas wallet). No `CIRCLE_NANO_API_KEY` anywhere.

**Finding: the dispatch path consumes NO API key.** The key is vestigial on verify+settle.
Corroborated by `env.ts:244-249` ("NO Circle account or API key is required to verify + record")
and the sibling rail precedent `env.ts:182-192` `isX402SettlementEnabled()` ("mirrors
isCircleNanoKernelEnabled"; "Until the payee is set, x402 stays DARK"). The established pattern
is to gate advertise/dispatch on the **settlement-capability** (recipient/payee) gate.

**→ LB-1 VERDICT = REPLACE** (not AND). AND-ing the vestigial key would leave a second
dark-disable knob (recipient-set/key-unset wrongly dark — the `unset/set` cell below), defeating
the coherence the register asked for. REPLACE is the only resolution that satisfies the §1 bar.

### Full misconfig truth table  {CIRCLE_NANO_API_KEY × SETTLEGRID_USDC_RECIPIENT}
Request = carries `x-circle-nano-auth` (so `isCircleNanoRequest`=true), no valid payment yet.
Both dispatch paths (unified `:332` enabledMap → `:385`; legacy `:472`) are equivalent by
contract — the table holds for both.

| KEY | RECIP | CURRENT dispatch (`isCircleNanoEnabled`) | CURRENT net result | POST-FIX dispatch (`isCircleNanoKernelEnabled`) | POST-FIX net result | Δ |
|---|---|---|---|---|---|---|
| set | set | TRUE → handler | proceeds (prod) | TRUE → handler | proceeds (prod) | **none — byte-identical (prod)** |
| set | unset | TRUE → handler → `:2015` 503 | circle-nano **consumes** req, 503 | FALSE → skip | **falls through to other rails as if disabled** | **FIX (the §1.3 key-only cell)** |
| unset | set | FALSE → skip | rail **dark despite serviceable** | TRUE → handler | circle-nano served (recipient-only) | **FIX (coherence; the AND-trap cell)** |
| unset | unset | FALSE → skip | not served (correct) | FALSE → skip | not served (correct) | none |

- **set/set (prod)** unchanged → zero prod delta (the INCREMENTAL invariant). ✓
- **set/unset** is the banner bug: pre = req routed into a 503; post = clean fall-through "exactly
  as when disabled" + a truthful (circle-nano-absent) advertisement. ✓ §1.3
- **unset/set** is why AND is wrong: REPLACE makes a recipient-only deploy work; AND would keep it
  dark. Pin this explicitly (LB-1 resolution). ✓ §1.3 "recipient-only ⇒ pinned explicitly"
- The `:2015` money boundary is **untouched** and remains as defense-in-depth (now provably
  shadowed by the re-keyed dispatch gate when recipient is unset).

## (c) DC-15 — cited-line re-verification (handoff §1.1 / §3)
Every line number cited in the handoff was re-read this session and **matches the live code**:
`circle-nano-proxy.ts:41` (def), `:50` (status arg), `route.ts:332` (enabledMap), `:472`
(dispatch), `route.ts:2015` (kernel handoff), `env.ts:260` (kernel gate), `settle:91`/`verify:93`
(already correct). **Two wording drifts** (recorded, do NOT change the fix):
1. `route.ts:332` is called *"the capability map — a PUBLISHED surface"*; mechanically it is the
   **internal `enabledMap`** inside `tryUnifiedAdapterDispatch` feeding `shouldDispatchUnified`
   — it is **not serialized to any public response**. The genuinely-published surfaces
   (mpp.json/openapi.json/learn) are static enums that don't read the gate. The accurate DC-18
   framing: the risk is **dispatch/routing incoherence** (the rail gets routed/advertised-for-
   dispatch when it can't serve), not a literal JSON-surface lie. "Capability map truthful"
   (§1.3) ⇒ the dispatch enablement reflects actual serveability (recipient).
2. `circle-nano-proxy.ts:50` is called *"the status surface"*; it is the `enabled:` arg to the
   **DEAD** `validateCircleNanoPayment` wrapper. Re-keying it is forward-coherence (zero runtime
   effect today).

## (d) DC-05 — forced-test sweep (which suites mock the gates / their envs)
| Suite | Touches | Impact of REPLACE | Edit needed |
|---|---|---|---|
| `proxy-equivalence.test.ts` | mirror `:170` (real `isCircleNanoEnabled`); synthetic `fullEnabledMap['circle-nano']=()=>true` `:239`; `beforeEach` `CIRCLE_NANO_API_KEY='cnano-test'` `:266` + `SETTLEGRID_USDC_RECIPIENT=''` `:274`; body-equiv `:1103` | mirror must re-key in lockstep with `:472` (comment `:163-166` mandates 1:1); with recip='' the re-keyed mirror would read DISABLED while synthetic stays ENABLED → break matching cases (`:358/:588/:599/:714`) & `assertEquivalent` | **YES** (see plan) |
| `circle-nano-proxy-settlement.test.ts` | `vi.mock('@/lib/circle-nano-proxy')` `:85-92` (stubs `isCircleNanoRequest`,`isCircleNanoEnabled`,…); recip set `:205`; recip unset `:311` → asserts **503** `:313` | the `:309` case currently pins the buggy 503 (req routed to `:2015`); post-fix dispatch keys on recipient → req **falls through** (never reaches `:2015`) | **YES** (re-pin `:309`) |
| `unified-dispatch.test.ts` | synthetic `'circle-nano':()=>true/false` `:259/:269` fed to `shouldDispatchUnified`; reads NO env | tests dispatch LOGIC, not the real gate | **none** |
| `x402-proxy-settlement.test.ts` | `:118` comment only; circle-nano disabled (envs unset) | stays disabled either gate | **none** |
| kernel `e2e-smoke.test.ts`/`route.test.ts` | mock `isCircleNanoKernelEnabled` for kernel routes | kernel routes untouched | **none** |

Notes for the plan:
- proxy-equivalence: the `:1103` "legacy === adapter" body case needs recip='' (wrapper omits
  discovery to match the bare adapter) → flip `beforeEach` recip to a VALID address and add a
  **local** `vi.stubEnv('SETTLEGRID_USDC_RECIPIENT','')` inside `:1103`. Import swap: drop
  `isCircleNanoEnabled` from the `@/lib/circle-nano-proxy` import (`:75`), add
  `isCircleNanoKernelEnabled` to the `@/lib/env` import (`:124-135`). circle-nano is never
  synthetically *disabled* in this suite, so no other case conflicts.
- circle-nano-proxy-settlement `:309`: the fall-through status must be confirmed EMPIRICALLY
  (other rails are env-disabled in this suite → it lands in the API-key flow). The fix is a
  fail-pre-fix re-pin: assert the response is NOT the circle-nano 503 / not `CIRCLE_NANO_NOT_
  CONFIGURED`, settlement (`H.execute`) NOT called, no circle-nano 402 emitted.

## TIER RE-CONFIRMATION — **INCREMENTAL** (no escalation trigger fires)
- No contract/schema/invariant change (aligns one existing gate to the stricter existing gate).
- No frozen-surface edit: settlement files, the all-or-nothing 402 emission, packages/mcp, the
  `:2015` money boundary, and kernel `:91/:93` are all UNTOUCHED.
- No new untrusted-input boundary (CLOSES a misconfig face). No determinism change.
- Money-ADJACENT but **gating-only**; prod (both-set) is **byte-identical** (truth-table row 1)
  → zero prod-visible delta. None of the escalation surfaces (verifier / 402 emission /
  settlement file / packages/mcp / prod-visible behavior) are touched. **Stays INCREMENTAL.**

## THE FIX (surface — detail goes in the DRAFT plan, ②)
Re-key the three enablement DECISIONS from `isCircleNanoEnabled` → `isCircleNanoKernelEnabled`:
`route.ts:332` (enabledMap), `route.ts:472` (legacy dispatch), `circle-nano-proxy.ts:50` (dead
wrapper, coherence); update the lockstep test mirror `proxy-equivalence.test.ts:170` + the two
suites above. Then `isCircleNanoEnabled` has **zero** live consumers → **keep-vs-delete is the
plan's open sub-decision** (delete fully closes DC-07's "two gates" but is more churn + touches
the dead wrapper + 1 test mock; keep is minimal but leaves a vestigial predicate/env). Remove the
now-unused `isCircleNanoEnabled` import at `route.ts:33` regardless (eslint no-unused).

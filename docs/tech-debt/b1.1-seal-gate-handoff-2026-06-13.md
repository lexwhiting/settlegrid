# B1.1 — circle-nano ENABLE-GATE SPLIT — ② SEAL-GATE HANDOFF (2026-06-13)

> **Self-contained for a FRESH session.** The chunk is **BUILT** and its **executable gate is GREEN**.
> Your job is ② — the independent, hostile, fresh-context, seal-DECIDING review of the BUILT DIFF, per
> the **verbatim prompt in §13**. Read this note end-to-end first, then run ②.

---

## 0. Confirm you're on the right tree (do this FIRST)
- Repo `/Users/lex/settlegrid`, branch `main`. The build is the **UNCOMMITTED working tree** atop
  `98159e04` (this chunk's handoff-doc commit), which sits atop the **(V) close `5ec73770`** atop `6465402c`.
- Verify: `git -C /Users/lex/settlegrid log --oneline -3` → HEAD `98159e04`; `git status --porcelain`
  shows ONLY the B1.1 working-tree changes (the 5 source/test files below + the docs + `.audit/`).
- The review object = the working-tree diff: `git -C /Users/lex/settlegrid diff` (and `--stat`/`--numstat`).
- ⚠ zsh: QUOTE bracketed paths (the route file is `apps/web/src/app/api/proxy/[slug]/route.ts`).
- ⚠ Never commit/push/deploy/set env (ALL founder-gated, post-②). DB read-only. vitest stays **2.1.9**.

## 1. What ② decides + the TIER
This is the seal-deciding review. **Tier = INCREMENTAL** — RE-CONFIRM against the realized diff and
**ESCALATE to HIGH-STAKES** (never silently lower) if the built code actually touched any of: the
verifier, the 402 all-or-nothing emission logic, any `apps/web/src/lib/settlement/*` file, packages/mcp,
the `:2015` money-boundary dark-gate, or **any behavior visible under the prod (both-env-set) config**.
The build-session evidence says none of these were touched (see §5/§6) — but verify independently.
Per §13: ③ (post-seal deep audit) is NOT warranted for an INCREMENTAL seal UNLESS your ② escalates the tier.

## 2. THE BAR (handoff §1, verbatim — what ② certifies)
*"A circle-nano 402 is advertised if and only if the rail can verify and settle what it advertises:
every proxy enablement decision keys on the recipient-based gate; a key-set/recipient-unset deploy serves
NO circle-nano 402 and a truthful capability map; prod-config behavior is byte-identical; the
advertised==verified architecture, packages/mcp, and every (V)-sealed settlement surface are untouched."*

## 3. THE BUILT DIFF (the review object) — 5 files
LB-1 = **REPLACE**: re-key every proxy circle-nano *enablement decision* from the API-key gate
`isCircleNanoEnabled()` (=`!!CIRCLE_NANO_API_KEY`) onto the recipient gate `isCircleNanoKernelEnabled()`
(=`!!getCircleNanoRecipient()`, env.ts). Post-edit line anchors (re-verify via the diff — earlier
comment additions shifted numbers):
1. **`apps/web/src/lib/circle-nano-proxy.ts`** (+4/−7): **DELETED** the `isCircleNanoEnabled` definition
   (now orphaned); re-keyed the **dead** `validateCircleNanoPayment` wrapper's `enabled:` arg (~`:47`) →
   `isCircleNanoKernelEnabled`; added `isCircleNanoKernelEnabled` to the `./env` import (~`:16`).
2. **`apps/web/src/app/api/proxy/[slug]/route.ts`** (+11/−4): unified `enabledMap` entry
   `'circle-nano'` (~`:336`) → `isCircleNanoKernelEnabled`; legacy dispatch (~`:479`,
   `if (… && isCircleNanoRequest(request))`) → `isCircleNanoKernelEnabled`; dropped the now-unused
   `isCircleNanoEnabled` import (~`:33`). **UNTOUCHED:** the `:2017-2030` dark-gate (the frozen money
   boundary — byte-identical), the handler body, the 402 generator, all other rails, the chain order.
3. **`apps/web/src/lib/env.ts`** (+4/−3): **COMMENT-ONLY** (~`:244-250`) — the sustained pre-build
   finding: the stale comment claiming "the legacy direct-proxy `isCircleNanoEnabled` … still gates on
   `CIRCLE_NANO_API_KEY`" was rewritten to the post-fix reality. **No logic/predicate/getter change.**
4. **`apps/web/src/lib/__tests__/proxy-equivalence.test.ts`** (+16/−10): the legacy-chain MIRROR
   (`legacyDetect`, ~`:171`) re-keyed IN LOCKSTEP with route.ts; import swap (drop `isCircleNanoEnabled`,
   add `isCircleNanoKernelEnabled` from `@/lib/env`); `beforeEach` (~`:275`) sets a VALID
   `SETTLEGRID_USDC_RECIPIENT`; the "Circle Nano: legacy === adapter" case (~`:1109`) locally stubs
   recipient='' so the wrapper's discovery enrichment matches the bare adapter.
5. **`apps/web/src/app/api/proxy/[slug]/__tests__/circle-nano-proxy-settlement.test.ts`** (+63/−7):
   `H.cnanoApiKeySet` toggle on the `isCircleNanoEnabled` mock; `:309` re-pinned (recipient-unset →
   fall-through **401**, NOT the old 503); NEW legacy unset/set case (→200); NEW **prod-default UNIFIED**
   describe block (`USE_UNIFIED_ADAPTERS` unset → defaults true) with set/unset (→401) + unset/set (→200)
   pinning `route.ts:336` end-to-end.

## 4. LB-1 = REPLACE + the misconfig truth table (mechanically probed — `.audit/b11-prebuild/probes.mjs`)
Why REPLACE (not AND): `handleCircleNanoProxy` (the dispatch target, route.ts ~`:1997`+) consumes **no
`CIRCLE_NANO_API_KEY`** anywhere — it gates on `isCircleNanoKernelEnabled()` (`:2017`) then verifies
recipient-only (`validateCircleNanoCredentialString`); `CIRCLE_NANO_API_KEY` is read in exactly ONE place
(the now-deleted `isCircleNanoEnabled`). AND-ing the vestigial key would leave a recipient-set/key-unset
rail wrongly dark. Sibling precedent: `env.ts` `isX402SettlementEnabled` ("mirrors isCircleNanoKernelEnabled").

| {API_KEY, RECIPIENT} | PRE-fix | POST-fix | Δ |
|---|---|---|---|
| set / set (PROD) | proceeds | proceeds | **none — byte-identical** |
| set / unset | dispatched → 503 | falls through → 401 | **the fix** |
| unset / set | dark (not served) | served | **coherence (AND-trap avoided)** |
| unset / unset | not served | not served | none |
Prod (both set) ⇒ both gates true ⇒ identical booleans ⇒ zero prod delta → INCREMENTAL.

## 5. FROZEN SPINE + SETTLED SCOPE (reject any fix that perturbs / re-litigates)
- **Frozen (must stay byte-stable):** every `apps/web/src/lib/settlement/*`; the 402 all-or-nothing
  emission + advertised==verified architecture; packages/mcp; route.ts `:2017` dark-gate; kernel
  `circle-nano/{settle,verify}/route.ts` (`:91`/`:93`); x402/AP2/other-rail dispatch; cron; pricing/payouts;
  migrations NONE; the proxy's NON-circle-nano behavior.
- **Settled (don't re-litigate without a concrete new trace):** banner item #2 (recipient off the hot
  gas wallet — FOUNDER/operator gate); P6 ops + the env-preflight line (a dark-rail diagnostic log belongs
  to P6, NOT here — §1-rejected); P7 hygiene; (G) tidies; the keep-vs-DELETE of `isCircleNanoEnabled`
  (DELETE was audit-ratified safe) and the no-new-log-key call (both dispositioned in the build plan §③).

## 6. EXECUTABLE GATE — clean-room results (re-run it, then hand to reviewers; don't re-derive facts)
Re-run command (apps/web):
```
cd /Users/lex/settlegrid/apps/web && npx tsc --noEmit && npx vitest run && npx next build && npx next lint
# from repo root: git diff --numstat ; git diff --stat -- packages/ ; git diff --stat -- 'apps/web/src/lib/settlement/'
```
Build-session results (expected): **tsc 0 · FULL vitest 4432 passed / 191 files / 0 fail / 0 skip
(baseline 4429 + 3 new) · next build 0 · next lint 0** (scoped to the 5 files: ✔ no warnings/errors) ·
numstat = exactly the 5 files above · `packages/` diff EMPTY · `settlement/` diff EMPTY.
- ⚠ P7 isolation flakes (`hop-rail-guard`, `gas-wallet-monitor`) gate on the **FULL run only** — don't
  run them in isolation and report flakes. The full run is green.
- Logs: `.audit/b11-build/{gate.log, gate-vitest.log, batch1-failpre.log, batch2-postfix.log, BUILD-STATUS.md}`.
- **Fail-pre-fix is captured EMPIRICALLY** (`batch1-failpre.log`): the 4 matrix-cell cases fail AT their
  asserts on pre-fix source (set/unset: `expected 503 not to be 503`; unset/set: `expected 401 to be 200`),
  green post-fix. Treat these as evidence, but the seal verdict is YOURS — re-derive what you must.

## 7. Lenses + recurrence classes to charge (INCREMENTAL floor = ≥3 lens-distinct fresh-context reviewers)
- **correctness / determinism** — the realized diff line-by-line; the re-key is identity in prod; the
  test mirror tracks route.ts 1:1; no other rail/precedence perturbed.
- **spec-conformance** — the §2 bar clause-by-clause; the full consumer census re-derived (zero
  straggler advertise/guard surface — incl. mpp.json/openapi.json/learn/buildMultiProtocol402/demo, all
  verified static enums); the misconfig matrix on BOTH dispatch paths.
- **core invariant (the moat here = advertise==serveable coherence + funds-adjacency)** — `:2017` money
  boundary byte-identical + now provably-shadowed defense-in-depth (no funds-path delta); no double/lost
  credit; prod byte-invariance; LB-1 REPLACE correctness (the dispatch path truly consumes no API key).
- **Recurrence classes** (`.audit/defect-ledger/INDEX.md`): **DC-07** (the headline — advertise-set vs
  guard-set drift) · **DC-13** (latent-in-prod / config-change) · **DC-18** (advertise/telemetry
  truthfulness) · **DC-15** (doc↔code drift) · **DC-05** (test-double fidelity / the mirror + the toggle) ·
  **DC-08** (fail-mode at the misconfig edge).
- The hostile/mechanical battery for this chunk = `.audit/b11-prebuild/probes.mjs` (truth-table + census);
  extend it rather than re-deriving by model. No NEW untrusted-input boundary was opened (the fix CLOSES
  a misconfig face; handler input-validation is frozen) — so the hostile-input surface is the env-combo
  matrix, already scripted.

## 8. MODEL & EFFORT POLICY (resolve BEFORE the fan-out, one pass — per §13's policy block)
- INCREMENTAL ⇒ the core-invariant **xhigh** clause does NOT apply (that's high-stakes-only). Seal-deciding
  reviewers + the integrator → **most capable model, effort high**; single-finding refuters → **one
  frontier tier down, effort high**. Set each spawned agent's model EXPLICITLY.
- In-harness precedent (the (T)/(U)/(V) seals, `.audit/v-seal/seal-review.mjs`): most-capable tier =
  **fable** for seal-deciding lenses + integrator; refuters = **opus**.
- ⚠ **In THIS chunk's build session, fable was reported INACCESSIBLE** (operator note) and the pre-build
  panel ran on **opus** lenses + opus refuters. So at launch: check fable accessibility. If accessible →
  fable lenses+integrator, opus refuters. If still inaccessible → per §13, PAUSE and queue the operator
  with the exact switch, OR run the fallback tier (opus) and **record it on the Policy line**. The
  integrator runs in YOUR session — if the session model is below the integrator's tier, queue that switch.

## 9. Reusable harness (adapt the hardened tail VERBATIM)
- This chunk's pre-build panel: **`.audit/b11-prebuild/b11-prebuild-review.mjs`** — a Workflow with a
  shared `CONTEXT`, lens array (Review phase, `schema`, explicit `model`), per-finding refuters (Verify
  phase, default-refuted), and the **hardened tail** (`deadLenses` degraded-run guard +
  `sustained = !verdict || refuted===false` → refuter-death = fail-safe SUSTAINED, no synthesizer —
  integrator decides). Clone it → `.audit/b11-seal/`, swap CONTEXT to "review the BUILT DIFF" (not the
  plan), set the lenses per §7, set models per §8.
- The (V) seal precedent (richer, 7 lenses): **`.audit/v-seal/seal-review.mjs`** — same hardened-tail shape.

## 10. Pre-build audit OUTCOME (context — ② reviews INDEPENDENTLY, but these were already weighed)
`wf_fe46aa73-9ad` (3 opus lenses + opus refuters): **0 dead lenses, 0 refuter-deaths, 1 sustained LOW +
12 refuted → PLAN_READY 0-blocking.** Folds applied: FOLD 1 (env.ts comment — sustained), FOLD 2 (the two
refuted MEDIUMs — prod-default unified-path e2e pins for `:336`; integrator elected to strengthen), FOLD 3
(`:2017` defense-in-depth annotation), FOLD 4 (positive 401 assertion). All 13 dispositions are in
`docs/tech-debt/b1.1-enable-gate-build-plan-2026-06-13.md` "## ③ AUDIT INTEGRATION". The build-session
§4.4 fresh-context self-verify (opus, read-only) returned **PASS**. Do NOT treat any of this as
certification — re-exhibit independently.

## 11. Reference docs
- Handoff (scope/bar/tier/LBs/frozen): `docs/tech-debt/b1.1-enable-gate-split-handoff-2026-06-12.md`
- Trace (census + LB walk + truth table + DC-05 sweep): `docs/tech-debt/b1.1-enable-gate-trace-2026-06-13.md`
- Build plan + audit integration (canonical where it disagrees with the trace):
  `docs/tech-debt/b1.1-enable-gate-build-plan-2026-06-13.md`
- Build evidence inventory: `.audit/b11-build/BUILD-STATUS.md`
- Original debt source: the B1.1 banner in `docs/tech-debt/b1.1-circle-nano-402-discovery-2026-05-31.md`
- Defect-class ledger: `.audit/defect-ledger/INDEX.md`
- Chunk-series memory: account-1 `…/memory/settlegrid-debt-chunks.md` (top block = this chunk's status).

## 12. After ② — the close (only if SEALED; founder-gated)
Founder-gated **path-scoped LOCAL commit** (`git commit -- <the 5 files + docs>`; NEVER push — pushes burn
the Vercel build limit) + close the B1.1 banner item **#1** + register/queue update (next: P6 ops → P7
hygiene → (G) tidies; P9 credit-finality = founder gate) + defect-ledger fold + memory. Banner item **#2**
(move `SETTLEGRID_USDC_RECIPIENT` off the hot gas wallet) stays a FOUNDER checklist item — NOT this chunk.
If your ② escalates to HIGH-STAKES, ③ (post-seal deep audit) becomes warranted before the close.

---

## 13. THE VERBATIM SEAL-GATE REVIEW PROMPT

The chunk is built and its executable gate is green. Before sealing, run an independent, hostile, fresh-context review of the actual diff — the review that DECIDES the seal. Assume a defect exists and work to exhibit it; do not certify by inspection. Scope is the BUILT CODE — not the plan, not the integrated system; do it fully and defer no finding to a later audit. The existence of any later phase must not reduce this one's rigor.

Inherit the chunk's risk tier from the handoff and RE-CONFIRM it against what was actually built; if the realized diff is riskier than the plan predicted (touched a frozen surface, opened an input boundary, etc.), ESCALATE — never silently lower. Size the review to the tier, but never below a floor of lens-distinct, fresh-context reviewers driving the REAL diff and live code: correctness/determinism, spec-conformance, and the core invariant (security, false-positive rate, data integrity — whatever the moat is). Reviewers report in coverage mode: every finding, including ones they are uncertain about or consider low-severity, each tagged with confidence and severity. They must not self-filter for importance — live reproduction downstream is the filter, and a surfaced finding that gets filtered beats a silently dropped defect. Run the project's full verification gate first in a clean, isolated run and hand the results to reviewers so none re-derive checkable facts. Hunt hardest at the new public boundaries under hostile or malformed input — the hostile-input battery itself stays a script. Charge each reviewer in isolation — their lens only, never the cadence.

[Model & effort policy — operational; resolve it BEFORE launching the fan-out, in one pass:
• Tiers: all seal-deciding reviewers and the integrator → most capable model, effort high; the core-invariant lens on a high-stakes chunk → xhigh. Single-finding refuters → one frontier tier down, effort high.
• SET each spawned agent's model explicitly per this policy — never silently inherit the session default.
• Effort: set per-agent where your harness allows it. Where effort is session-level only and a role requires more than the current setting, PAUSE NOW and queue the operator with the exact switch needed; resume only on their confirmation. Above policy = cost note; a decisive role below policy is forbidden.
• The integrator runs in your own session: if the session model is below the integrator's tier, queue that switch before integrating.
• If a reviewer's run is declined by a safety classifier (a refusal, not an error), re-spawn that lens once on the fallback tier and record it on the Policy line — a lens silently lost to a refusal is a coverage hole, not an acceptable outcome.
• Mechanics: scripts. Run the fan-out parallel/async where the environment permits; keep working while reviewers run, and intervene if one goes off track or is missing context it needs. Record the outcome on the Policy line of the cadence status.]

Integrate as the single integrator, triaging by severity and confidence: reproduce every sustained high- and medium-severity finding live — failing against the built code, then passing after the fix — before it lands; batch and spot-reproduce lows. Each fix re-enters a fresh review of its class (a purely mechanical fix may take a proportionate reduced re-review — record the calibration). Reject any fix that pulls in deferred work, gold-plates, or perturbs a frozen surface. If a structured result is lost to a tool or transport failure, integrate the completed parts by hand and re-run every load-bearing claim live. Before reporting, audit each claim against a tool result from this session; report only work you can point to evidence for — if something is not yet verified, say so explicitly. Seal only when the gate is green, zero high-severity findings are open, and the reviewers' evidence supports it; then do the bookkeeping (status, log, derived snapshots, the defect-class ledger, the next handoff). If you cannot seal, say so plainly with the blocking findings. Close with:

━━ CADENCE STATUS ━━
Done:  ② seal-gating review → [SEALED | BLOCKED]
Tier:  [high-stakes | incremental]  (escalated? y/n)
Policy: [applied | ⚠ awaiting operator switch: <model/effort + role>]
▶ NEXT: [high-stakes] paste ③ the post-seal deep audit · [incremental] ③ not warranted — proceed to ① for the next chunk.

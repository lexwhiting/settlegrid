# B1.1 — circle-nano ENABLE-GATE SPLIT (advertised == servable) — CHUNK HANDOFF (2026-06-12)

> **Self-contained handoff for a FRESH session. Read end-to-end before touching anything.**
> Closes the B1.1 carried-debt banner item **#1** (`b1.1-circle-nano-402-discovery-2026-05-31.md`
> ⚠ item 1): the proxy routes circle-nano on `isCircleNanoEnabled()` (`CIRCLE_NANO_API_KEY`)
> while discovery + the verifier gate on `SETTLEGRID_USDC_RECIPIENT`
> (`isCircleNanoKernelEnabled()`) — a key-set/recipient-unset deploy serves a DARK
> (unpayable) 402. Item #2 of that banner (recipient off the hot gas wallet) is FOUNDER
> CONFIG — an operator gate, NOT this chunk.

## ⛔ 0. HARD PRECONDITION — the (V) ③ + CLOSE must be COMPLETE before this chunk's ① trace/plan/audit/build start
The (V) chunk (register P5+P8+P8(g)) is ②-SEALED (2026-06-12) but ③ (post-seal deep audit)
is PENDING and its tree is UNCOMMITTED. The prior cadence status' NEXT is ③ — run it first
(`docs/tech-debt/v-pending-lifecycle-deep-audit-handoff-2026-06-12.md`), then the (V) close
(founder-gated path-scoped LOCAL commit + capstone + register close + runbook + ledger).
Verify before anything here: `git log --oneline -3` shows the (V) close commit atop
`6465402c`; `git status` is CLEAN of `apps/web/src/lib/settlement/*`. Building (or even
tracing) atop the uncommitted (V) tree contaminates the commit boundary AND grounds the
trace against files ③ may still modify (DC-15). THIS handoff was drafted pre-③ — its cited
surfaces (`circle-nano-proxy.ts`, proxy `route.ts`, `env.ts`) are OUTSIDE the (V) diff, but
re-verify every cited line at trace time.

## 0.5 Source-of-truth confirmation (derived 2026-06-12 — re-verify cited lines, not the queue)
Queue: the ③ register (`s-deep-audit-register-2026-06-10.md`) + the (U)/(V) handoffs §0.5 +
memory all agree: after (V) → **B1.1 (forbidden-dilution INCREMENTAL, stays standalone)** →
P6 ops → P7 hygiene → (G) tidies; P9 credit-finality = FOUNDER decision gate. Prior-chunk
"next" cross-check: the (V) ② cadence status' NEXT = ③ — hence ⛔§0 (the sequencing
dependency, flagged). Scope authority for THIS chunk: the B1.1 doc's carried-debt banner +
the repeated rejected-merge entries in the (G)/(U)/(V) handoffs (each calls it "INCREMENTAL,
different seam, stays standalone").

## 1. SCOPE (record verbatim; sized at ① — the chunk stays AS PLANNED, standalone)
**Align every proxy circle-nano enablement decision onto the recipient-based gate so the
402 is advertised IF AND ONLY IF the rail can verify what it advertises:**
1. Census-confirmed consumer set of the API-key gate (`isCircleNanoEnabled`,
   `apps/web/src/lib/circle-nano-proxy.ts:41`): proxy `route.ts:332` (the capability map —
   a PUBLISHED surface), `route.ts:472` (the dispatch gate `isCircleNanoEnabled() &&
   isCircleNanoRequest(request)`), and `circle-nano-proxy.ts:50` (the status surface).
   The recipient-based gate (`isCircleNanoKernelEnabled`, `apps/web/src/lib/env.ts:260`)
   already governs the kernel verify/settle routes and proxy `route.ts:2015`. The trace
   censuses BOTH gates' full consumer sets (incl. tests, packages/, any status/manifest
   surface) before the plan fixes anything.
2. Re-key the proxy decisions (capability map + dispatch + status) to the recipient-based
   predicate — the exact composition (replace vs AND-with-key) is the trace's LB-1 decision
   (below), resolved from what the dispatch path actually consumes downstream
   (`validateCircleNanoCredentialString`, the credential parse, the kernel handoff).
3. Fail-pre-fix tests pinning the FULL misconfig matrix {CIRCLE_NANO_API_KEY,
   SETTLEGRID_USDC_RECIPIENT} × {set, unset}: key-only ⇒ NO circle-nano 402, capability map
   truthful (DC-18), request falls through to the other rails exactly as when disabled;
   both-set ⇒ byte-identical to today (prod has both — ZERO prod behavior delta);
   recipient-only ⇒ whatever the LB-1 resolution says, pinned explicitly.
**REJECTED merges:** banner item #2 (founder config — operator gate); P6 ops (different
seam: reconciler observability; the env-preflight line stays a founder-checklist item);
P7 hygiene (different seams); (G) tidies; ANY settlement-file touch ((V)-sealed); the 402
generator's all-or-nothing emission logic and packages/mcp (sealed B1.1 architecture —
advertised==verified BY CONSTRUCTION; the core generator stays crypto-constant-free);
migrations; pushes/deploys.
**Scoping decision (explicit):** stays standalone AS PLANNED. The register/handoffs forbid
diluting B1.1 into other chunks, nothing else in the queue shares the
enablement-coherence seam, and the only adjacent banner item crosses an operator gate.
**The bar:** *"A circle-nano 402 is advertised if and only if the rail can verify and
settle what it advertises: every proxy enablement decision keys on the recipient-based
gate; a key-set/recipient-unset deploy serves NO circle-nano 402 and a truthful capability
map; prod-config behavior is byte-identical; the advertised==verified architecture,
packages/mcp, and every (V)-sealed settlement surface are untouched."*

## 2. TIER: **INCREMENTAL** (re-confirm at ① against the realized trace)
Triggers checked: no contract/schema/invariant change (aligns an existing gate to the
stricter existing one); no frozen-surface edit (settlement files untouched; the 402
generator architecture preserved, not modified); no new untrusted-input boundary (CLOSES a
misconfig face); no determinism/reproducibility change; money-ADJACENT but gating-only —
prod has both vars set, so the prod-visible delta is zero by construction. The source of
truth has classified B1.1 INCREMENTAL consistently ((G)/(U)/(V) handoffs). **ESCALATE to
HIGH-STAKES if the trace shows the fix must touch:** the verifier, the all-or-nothing 402
emission logic, any `apps/web/src/lib/settlement/*` file, packages/mcp, or any behavior
visible under the prod (both-set) config — never silently lower; uncertain ⇒ escalate.

### The LOAD-BEARING decisions most likely to be SILENTLY WRONG
**LB-1 — the gate composition (replace vs AND).** "Align to `isCircleNanoKernelEnabled()`"
can mean (a) REPLACE the key gate, or (b) require BOTH. Wrong toward (a): if the dispatch
path downstream genuinely consumes `CIRCLE_NANO_API_KEY` (credential validation), a
recipient-only deploy dispatches requests into a path that 500s/misbehaves — dark
breakage that both-set tests never see. Wrong toward (b): if the key is vestigial on this
path, AND-ing it preserves a second way to dark-disable a configured rail (revenue off)
and leaves two knobs where the register asked for coherence. The trace MUST resolve this
from the dispatch's actual downstream consumption (what does `:472`'s branch do with the
credential? does the kernel handoff at `:2015` re-check?) — not from the banner's wording.
Every cell of the misconfig matrix gets a fail-pre-fix or pass-pre+post pin (DC-13:
misconfig behavior is LATENT — test as latent).
**LB-2 — consumer-set completeness (DC-07, the headline class: two gates for one
capability IS the duplicated-knowledge defect).** Aligning the dispatch but not the
capability map (or the status surface, or a manifest/openapi/learn surface the census
missed) passes every dispatch test while the PUBLISHED map lies (DC-18). The census must
be MECHANICAL (grep both gate names tree-wide incl. packages/ and tests) and the plan must
state the post-fix consumer count for each gate with zero stragglers.

## INTENT
*Why:* the rail's enablement truth is currently split across two env vars with different
fail shapes — the misconfig face (key set, recipient unset) serves payers a 402 they
CANNOT pay (a buyer-facing dead end the B1.1 banner has carried since 2026-05-31). *Who
consumes:* external payers hitting the proxy 402 (never see an unpayable challenge); the
founder's env/deploy checklist (one coherent knob instead of a coupled pair — feeds the
P6 env-preflight line); the capability map's consumers (truthful advertisement). *What it
enables:* closes the B1.1 banner's last code item; simplifies the P6 ops chunk's
env-preflight scope; after this, the queue is P6 ops → P7 hygiene → (G) tidies.

## 3. Ground state + frozen surfaces
- Repo `/Users/lex/settlegrid`, branch `main`. Build atop the (V) CLOSE commit (⛔§0).
  Baseline gate at the (V) seal: apps/web tsc **0** · FULL vitest **4428 / 191 / 0** ·
  build **0** · packages byte-stable (re-anchor after the close — ③ may add tests).
  ⚠ register-P7 isolation flakes (hop-rail-guard, gas-wallet-monitor): gate on the FULL
  suite only. ⚠ vitest stays 2.1.9. zsh: QUOTE bracketed paths. DB read-only; never
  push/deploy/set env (ALL founder-gated).
- **UNFROZEN (the licensed surface):** `apps/web/src/lib/circle-nano-proxy.ts` (the gate
  + status surface), `apps/web/src/app/api/proxy/[slug]/route.ts` (capability map :332 +
  dispatch :472 — line numbers from 2026-06-12, re-verify), `apps/web/src/lib/env.ts`
  ONLY if the trace concludes the predicate belongs there (prefer consuming the existing
  helper), their test files, docs.
- **BYTE-STABLE spine (zero behavioral delta; reject any "fix" that perturbs it):** every
  `apps/web/src/lib/settlement/*` file ((V)-sealed; commit lands at the (V) close); the
  402 generator's all-or-nothing emission + the advertised==verified architecture +
  packages/mcp (crypto-constant-free rule); the kernel verify/settle routes' own
  `isCircleNanoKernelEnabled` gates (:91/:93 — already correct); x402/AP2/other rails'
  dispatch; the cron routes; pricing/payouts; migrations NONE; the proxy's NON-circle-nano
  behavior byte-identical.
- Behavior pins: BOTH-SET config ⇒ the full proxy flow byte-identical (the prod
  invariant); the capability map's other entries untouched; no new env var, no new log
  key unless the trace shows the dark-rail case needs ONE structured line (decide at
  trace, flag in the plan).

## 4. THE ARC (do not skip/reorder — the (T)/(U)/(V) pattern, INCREMENTAL-sized)
1. **Scope-confirm trace** (`b1.1-enable-gate-trace-<date>.md`): (a) mechanical census —
   every consumer of BOTH gates (grep `isCircleNanoEnabled|isCircleNanoKernelEnabled`
   tree-wide incl. packages/, tests, manifests/openapi/learn surfaces); (b) the LB-1
   downstream walk — what `:472`'s branch and the kernel handoff actually consume
   (credential parse, validateCircleNanoCredentialString, `:2015` re-check) → the
   replace-vs-AND verdict with the misconfig matrix's full truth table; (c) DC-15 line
   re-verification (this handoff's citations); (d) DC-05 forced-test sweep (which suites
   mock the gates/env — route tests, circle-nano-proxy tests). 
2. **Build plan** (DRAFT until audited): per-file recipes; fail-pre-fix tests for every
   matrix cell that changes + pass-pre+post pins for the prod cell; behavior pins (§3);
   gates (tsc 0 / FULL vitest baseline+N / build 0 / eslint 0 / numstat ⊆ licensed files /
   packages diff 0).
3. **MANDATORY pre-build audit — INCREMENTAL shape** (closes before ANY code): mechanical
   probes FIRST (the census, the matrix truth table as a script over the real predicates);
   then a REDUCED lens set (2–3 fresh-context lenses, single-pass, COVERAGE against the §1
   bar — report anything that could cause incorrect behavior, a failing check, or a
   misleading result; omit only pure style) + single-finding refuters. Adapt the hardened
   tail VERBATIM from `.audit/v-seal/seal-review.mjs` (degraded-run guard, refuter-death =
   fail-safe SUSTAINED) → `.audit/b11-prebuild/`. **RECURRENCE LENSES**
   (`.audit/defect-ledger/INDEX.md`): **DC-07** (the headline — two gates, one capability)
   · DC-13 (misconfig-latent; test as latent) · DC-18 (the capability map is a published
   surface) · DC-15 (doc↔code drift) · DC-05 (env/gate mock fidelity). Defer NO finding;
   PLAN_READY 0-blocking before any code. All sustained findings re-confirmed live by the
   implementer and folded BEFORE build code.
4. **Single-writer build + interval self-verification:** after EACH batch, a fresh-context
   read-only subagent diffs the work vs THIS handoff §1/§2/§3 (scope, the two LBs, frozen
   spine); fail-pre-fix proven EMPIRICALLY (captures to `.audit/b11-build/`).
5. **Executable gate** → END the build session with a CADENCE-STATUS report flagging
   readiness for ② (seal-gating review, INCREMENTAL floor — lens-distinct fresh-context
   reviewers; ③ NOT warranted unless ② escalates the tier).
6. At close (after ②): founder-gated path-scoped LOCAL commit + the B1.1 banner item-#1
   close + register/queue update + ledger folding + memory. Banner item #2 (recipient off
   the hot wallet) stays on the founder checklist.

## 5. Model & effort policy → THIS harness (resolve before fan-out; record on the Policy line)
- INCREMENTAL tier: lenses + single-finding refuters = **opus** (one frontier tier down),
  effort high. Orchestration/integration runs in-session on **fable** (above policy = cost
  note, not a blocker). NO per-agent effort knob exists in this harness — record the
  intent on the Policy line (the (T)/(U)/(V) precedent; model tier is the applied
  control). If ② escalates the tier, the ② panel re-sizes to the HIGH-STAKES floor
  (fable lenses).
- A lens lost to a safety-classifier REFUSAL (not an error): re-spawn once on the fallback
  tier and record it. Mechanics = scripts, never model calls; fan-out parallel/async.

## 6. Conduct (binding — the (T)/(U)/(V) wording)
(a) self-verify per §4.4 with fresh-context subagents at every batch boundary; (b) ground
EVERY progress claim in a tool result from the session — report only evidence-backed work;
(c) act once you have enough information — the queue archaeology is DONE, the
(T)/(S)/(U)/(V) decisions are SETTLED, do not re-derive or re-litigate them; (d) NEVER
stop, summarize, or suggest a new session on account of context limits — the harness
manages context; (e) end the build session with the CADENCE-STATUS report the moment the
executable gate is green.

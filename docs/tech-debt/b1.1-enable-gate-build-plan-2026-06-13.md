# B1.1 — circle-nano ENABLE-GATE SPLIT — BUILD PLAN (PLAN_READY — audit closed 0-blocking)

> ② of the ARC. **PLAN_READY** — the §4.3 pre-build audit (`wf_fe46aa73-9ad`) closed at **0 blocking**
> (1 sustained LOW + 12 refuted; 0 dead lenses; 0 refuter-deaths). Folds integrated below in
> "## ③ AUDIT INTEGRATION". Grounded in the trace `b1.1-enable-gate-trace-2026-06-13.md`.

## The bar (handoff §1, verbatim)
*"A circle-nano 402 is advertised if and only if the rail can verify and settle what it
advertises: every proxy enablement decision keys on the recipient-based gate; a
key-set/recipient-unset deploy serves NO circle-nano 402 and a truthful capability map;
prod-config behavior is byte-identical; the advertised==verified architecture, packages/mcp, and
every (V)-sealed settlement surface are untouched."*

## Resolved decisions (from the trace — SETTLED, not re-litigated here)
- **LB-1 = REPLACE.** `handleCircleNanoProxy` (the dispatch target) consumes NO `CIRCLE_NANO_API_KEY`
  anywhere; it gates on `isCircleNanoKernelEnabled()` (`:2015`) and verifies recipient-only
  (`validateCircleNanoCredentialString`). `CIRCLE_NANO_API_KEY` is read in exactly one place
  (`isCircleNanoEnabled`). AND-ing would leave the recipient-set/key-unset rail wrongly dark.
- **LB-2** complete consumer set of the API-key gate: `route.ts:332` (unified enabledMap) +
  `route.ts:472` (legacy dispatch), lockstep with mirror `proxy-equivalence.test.ts:170`; plus the
  DEAD `validateCircleNanoPayment` wrapper arg (`circle-nano-proxy.ts:50`). Zero published-surface
  stragglers (mpp.json/openapi.json/learn = static enums, gate-free).
- **Tier = INCREMENTAL** — no escalation trigger fires (prod byte-identical; no frozen surface,
  verifier, 402-emission, settlement file, or packages/mcp touched).

## OPEN sub-decisions for the audit (③) to ratify
1. **Keep-vs-DELETE the orphaned `isCircleNanoEnabled`.** Re-keying `:50` (per handoff §1.2)
   orphans the predicate (0 consumers). **Recommend DELETE** (`circle-nano-proxy.ts:41-43`) — a
   zero-consumer second gate IS the DC-07 duplicated-knowledge defect; deletion makes regression
   impossible and fully satisfies the bar. Conservative alternative: keep it (a dead export +
   vestigial `CIRCLE_NANO_API_KEY`). The DEAD `validateCircleNanoPayment` wrapper itself is NOT
   deleted (pre-existing dead code, out of this seam) — only its `:50` arg is re-keyed.
2. **No new log key.** A dark-rail diagnostic (key-set/recipient-unset) is an **env-preflight**
   concern — §1 explicitly reserves that for **P6 ops** ("the env-preflight line stays a
   founder-checklist item"). Adding it here dilutes B1.1 into P6's seam. **Recommend: no new log
   key, no new env var** (honors §3 default). The fix's job is enablement coherence, not misconfig
   telemetry.
3. **DC-15 wording drifts** (recorded in the trace; do NOT change the fix): `route.ts:332` is the
   internal unified-dispatch `enabledMap` (not a serialized "published surface"); `:50` is the arg
   to a DEAD wrapper (not a live "status surface"). The fix (re-key all three) is unchanged.

## Per-file recipes (exact edits)

### F1 — `apps/web/src/lib/circle-nano-proxy.ts` (UNFROZEN)
- **`:19`** add the kernel gate to the env import:
  `import { getAppUrl, getCircleNanoRecipient } from './env'`
  → `import { getAppUrl, getCircleNanoRecipient, isCircleNanoKernelEnabled } from './env'`
- **`:50`** re-key the dead wrapper's arg: `enabled: isCircleNanoEnabled(),`
  → `enabled: isCircleNanoKernelEnabled(),`
- **`:40-43`** DELETE the orphaned `isCircleNanoEnabled` fn + its comment (pending audit decision #1).

### F2 — `apps/web/src/app/api/proxy/[slug]/route.ts` (UNFROZEN)
- **`:33`** drop the now-unused import:
  `import { isCircleNanoRequest, isCircleNanoEnabled, validateCircleNanoCredentialString, generateCircleNano402Response } from '@/lib/circle-nano-proxy'`
  → remove `isCircleNanoEnabled,` (keep the other three). `isCircleNanoKernelEnabled` already
  imported at `:47`.
- **`:332`** enabledMap: `'circle-nano': isCircleNanoEnabled,` → `'circle-nano': isCircleNanoKernelEnabled,`
  (+ one-line comment: keys on the settlement-capability gate so dispatch == serveable; mirrors
  `isX402SettlementEnabled`).
- **`:472`** legacy dispatch: `if (isCircleNanoEnabled() && isCircleNanoRequest(request)) {`
  → `if (isCircleNanoKernelEnabled() && isCircleNanoRequest(request)) {`
  (+ a matching one-line comment).
- **UNTOUCHED:** `:2015` dark-gate (frozen money boundary — now defense-in-depth), the handler body,
  `generateCircleNano402Response`, all other rails.

### F3 — `apps/web/src/lib/__tests__/proxy-equivalence.test.ts` (test — consistency, not source pin)
- **`:75`** drop `isCircleNanoEnabled` from the `@/lib/circle-nano-proxy` import (keep
  `isCircleNanoRequest`, `generateCircleNano402Response as legacyCnano`).
- **`:124-135`** add `isCircleNanoKernelEnabled` to the `@/lib/env` import group.
- **`:170`** re-key the legacy mirror in lockstep with F2/`:472`:
  `if (isCircleNanoKernelEnabled() && isCircleNanoRequest(request)) return { matched: 'circle-nano' }`
- **`:274`** beforeEach: `SETTLEGRID_USDC_RECIPIENT=''` → a VALID address (`'0x' + '9'.repeat(40)`)
  so the kernel gate is OPEN → matching cases (`:358/:588/:599/:714`) stay green. Update the
  `:267-273` comment (recipient now set; discovery-less body-equivalence handled locally at `:1103`).
  `:266 CIRCLE_NANO_API_KEY` stub left as-is (now vestigial; note inline).
- **`:1103`** ("Circle Nano: legacy === adapter"): prepend `vi.stubEnv('SETTLEGRID_USDC_RECIPIENT','')`
  so the wrapper omits discovery and matches the bare adapter (with a one-line why).

### F4 — `apps/web/src/app/api/proxy/[slug]/__tests__/circle-nano-proxy-settlement.test.ts` (test — the matrix pins)
- **mock `:85-92`** make the API-key predicate toggleable for the unset/set pin: add
  `cnanoApiKeySet: true` to `H`; change `isCircleNanoEnabled: () => true,`
  → `isCircleNanoEnabled: () => H.cnanoApiKeySet,`. (Needed so the test still satisfies the pre-fix
  `route.ts` import; post-fix it is vestigial-but-harmless.) Reset `H.cnanoApiKeySet = true` in the
  suite `beforeEach` (`:202`).
- **`:309` re-pin (set/unset cell — fail-pre-fix):** retitle to "recipient unset (rail dark) →
  circle-nano NOT dispatched, falls through; money path untouched". Keep `RECIPIENT=''`. Replace the
  503 assertions with: `res.status !== 503`, `body.code !== 'CIRCLE_NANO_NOT_CONFIGURED'`,
  `H.execute` NOT called, `H.genCircleNano402` NOT called. (Exact fall-through status confirmed
  empirically and pinned.)
- **NEW case (unset/set cell — fail-pre-fix):** "API key unset but recipient set → circle-nano STILL
  serves (gate keys on recipient, not API key)": `H.cnanoApiKeySet=false`; recipient set (beforeEach);
  settle mock → settled; assert `res.status === 200` + tx-hash header (mirrors `:215`).
- **UNTOUCHED main cases (set/set — prod pass-pre+post):** `:215/:255/:266/:275/:285/:298/:321`.

## Misconfig matrix → test mapping (DC-13: test as LATENT)
| Cell {KEY,RECIP} | Behavior | Pin | Suite |
|---|---|---|---|
| set/set (prod) | served — **byte-identical** | pass-pre+post | settlement `:215` etc. (unchanged) |
| set/unset | falls through (was 503) | **fail-pre-fix** | settlement `:309` (re-pinned) |
| unset/set | served (was dark) | **fail-pre-fix** | settlement NEW case |
| unset/unset | not served | covered-by-construction (both gates false) | implicit |

## Behavior pins (§3)
- BOTH-SET ⇒ full proxy flow byte-identical (truth-table row 1; the settlement main cases prove it).
- The capability map's other entries (`:324-339`) and every non-circle-nano rail untouched.
- `:2015` money boundary, `generateCircleNano402Response`, packages/mcp, all settlement files,
  kernel `:91/:93` UNTOUCHED. Migrations NONE. No new env var / log key.

## Gates (executable, ⑤)
- `npx tsc --noEmit` (apps/web) = **0**.
- `vitest run` (FULL) = **4430 pass / 0 fail** (empirical baseline re-anchored this session =
  **4429 pass / 191 files / 0 fail / 0 skip**; +1 new unset/set case; the `:309` re-pin modifies
  in place. P7 isolation flakes hop-rail-guard/gas-wallet-monitor gate on the FULL run only).
- `next build` = **0**. `next lint` = **0** on touched files.
- `git diff --numstat` source set ⊆ {`circle-nano-proxy.ts`, `route.ts`, `proxy-equivalence.test.ts`,
  `circle-nano-proxy-settlement.test.ts`} (+ docs + `.audit/`). **packages/ diff = 0.**

## Build sequence (④ — single-writer + interval self-verification)
1. **Batch 1 — fail-pre-fix demo:** apply F4 (settlement test: mock toggle + `:309` re-pin + new
   case) → run settlement suite against CURRENT source → **capture FAILURE** of the two cells to
   `.audit/b11-build/`. Fresh-context self-verify vs §1/§2/§3.
2. **Batch 2 — source fix:** apply F1 + F2 → fresh-context self-verify (diff vs handoff §1/§2/§3:
   scope, LB-1 REPLACE, LB-2 completeness, frozen spine) → run settlement suite → **capture PASS** +
   `tsc 0`.
3. **Batch 3 — equivalence consistency:** apply F3 (atomic: mirror + beforeEach recipient + `:1103`)
   → self-verify → run proxy-equivalence + unified-dispatch suites → PASS.
4. **Batch 4 — full gate ⑤:** tsc + FULL vitest + build + lint + numstat + packages-diff → green →
   CADENCE-STATUS report flagging readiness for ②.

## What this is NOT (rejected merges — handoff §1)
banner item #2 (founder/operator); P6 ops + the env-preflight line; P7 hygiene; (G) tidies; ANY
settlement-file touch; the 402 all-or-nothing emission + packages/mcp; migrations; pushes/deploys.

---

## ③ AUDIT INTEGRATION — PLAN_READY (wf_fe46aa73-9ad)
3 fresh-context **opus** lenses (coverage) + opus refuters (default-refuted). Per-lens: gate-coherence=5,
frozen-spine=5, test-fidelity=3 (13). **0 dead lenses, 0 refuter-deaths, 1 SUSTAINED, 12 refuted.**
Mechanical probes (ran pre-panel): truth-table ALL PASS, census zero stragglers. Disposition of EVERY
finding (defer none):

**FOLD 1 [SUSTAINED, low] stale `env.ts:247-249` comment.** Re-confirmed live: it asserts "the legacy
direct-proxy `isCircleNanoEnabled` … still gates on `CIRCLE_NANO_API_KEY` — a separate, untouched
surface" — FALSE once `isCircleNanoEnabled` is deleted (DC-07/DC-15 re-seed hazard). → **F1 now also
rewrites that comment** to: the kernel rail's recipient gate is the sole circle-nano enablement gate;
`CIRCLE_NANO_API_KEY`/`isCircleNanoEnabled` removed (B1.1); the var is now unread. Comment-only edit
inside the licensed env.ts surface (consumes the existing helper; no predicate added).

**FOLD 2 [MEDIUM ×2, refuted — elected to strengthen] prod-default UNIFIED path (`:332`) never
exercised e2e with the real recipient gate.** Refuters confirmed the FACTS (useUnifiedAdapters() defaults
TRUE → prod runs the unified path; both F4 pins force `USE_UNIFIED_ADAPTERS='false'` = legacy `:472`;
the equivalence test uses synthetic predicates so it can't catch a `:332`↔`:472` divergence) but refuted
as "no plan defect" (the plan re-keys both). Integrator judgment: the prod-active path deserves an
executable pin, and it is feasible (`decideUnifiedDispatch` detects circle-nano from the real
`x-circle-nano-auth` header via `protocolRegistry.detect`, routing through `:332` to the same handler).
→ **F4 adds a "prod-default UNIFIED path" describe block** (`USE_UNIFIED_ADAPTERS` unset → defaults true)
with set/unset → fall-through (pre-fix 503 via `:332`=key; post fall-through via `:332`=recipient) and
unset/set → served. These pin `:332`'s binding e2e and catch a future `:332`/`:472` divergence.

**FOLD 3 [MEDIUM, refuted — accept + annotate] `:309` re-pin drops the sole e2e test of `:2015`.**
Refutation SOUND: post-fix BOTH dispatch paths require recipient-set, so `:2015`'s `!isCircleNanoKernel
Enabled()` 503 branch is unreachable-by-construction (provably-shadowed defense-in-depth, byte-identical/
unedited per §3); its 503 semantics stay covered by the kernel `settle:91`/`verify:93` suites. → add a
clarifying comment in the re-pinned `:309` noting `:2015` is now shadowed defense-in-depth.

**FOLD 4 [low, refuted — cheap hardening] `:309` NOT-503 assertion weak.** → assert the fall-through
status POSITIVELY (confirmed empirically at build), not merely `!= 503`.

**Refuted, NO fold (sound refutations, recorded):** unified-`:332` "note-only" coverage nit (subsumed by
FOLD 2); census under-enumerates sessions `z.enum`/demo-sandbox (both verified gate-free → LB-2 stands);
vitest count split (suggestedFix already in plan); lost `proxy.circle_nano_not_configured` diagnostic
(P6 env-preflight seam, §1-rejected; pure-style); `circle-nano-402-discovery.test.ts` omitted from the
sweep table (imports only the generator, gate-free — traceability nit); truth-table x402-precedence
sub-case (self-defeating — correct behavior, misconfig-only); env.ts DC-15 "under both keep+delete"
(refuted — under KEEP the comment stays accurate; FOLD 1 covers the DELETE path).

### Revised count + sequence (supersedes the DRAFT specifics)
- **Vitest gate: 4429 → 4432** (+3 net new `it`: legacy unset/set, unified set/unset, unified unset/set;
  `:309` re-pinned in place). proxy-equivalence: modified setup/mirror, no count change.
- **Build batches (the `isCircleNanoEnabled` DELETE couples all its importers — they move together):**
  1. **Fail-pre-fix demo:** apply F4 (mock toggle + `:309` re-pin + 3 new cases) → run circle-nano-proxy-
     settlement against PRE-FIX source → capture the 4 reds to `.audit/b11-build/` (compiles: the module is
     fully mocked, pre-fix route.ts still imports `isCircleNanoEnabled`). Fresh-context self-verify.
  2. **Source + coupled imports (ATOMIC compile-unit):** F1 (re-key `:50`, DELETE `:40-43`, add env import,
     rewrite `env.ts:247-249` comment) + F2 (re-key `:332`/`:472`, drop `:33` import) + F3 (drop `:75`
     import, add `isCircleNanoKernelEnabled`, re-key mirror `:170`, flip `beforeEach` recipient, `:1103`
     local override). Fresh-context self-verify (diff vs §1/§2/§3 + confirm BOTH `:332` and `:472`
     re-keyed). Run tsc + circle-nano-proxy-settlement + proxy-equivalence + unified-dispatch → 4 reds now
     GREEN + equivalence green.
  3. **Full gate ⑤:** tsc + FULL vitest (=4432) + build + lint + numstat + packages-diff → CADENCE-STATUS.

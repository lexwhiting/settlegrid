# B1.1 — circle-nano ENABLE-GATE SPLIT — ② SEAL (SEALED, 2026-06-13)

> ③ of the ARC. **SEALED — INCREMENTAL, not escalated.** Executable gate GREEN; the independent
> hostile seal-gating panel returned **0 sustained / 0 high / 0 medium** (10 coverage-mode LOWs, all
> adversarially refuted). Per the cadence, ③ (post-seal deep audit) is **NOT warranted** for an
> un-escalated INCREMENTAL seal. The founder-gated path-scoped LOCAL commit is pending (see "The close").

## Verdict — SEALED
A circle-nano 402 is now advertised/dispatched **iff** the rail can verify+settle it. Every proxy
enablement decision keys on the recipient (settlement-capability) gate `isCircleNanoKernelEnabled()`:
the unified enabledMap (`route.ts:336`), the legacy dispatch (`route.ts:479`), and the dead
`validateCircleNanoPayment` wrapper arg. The API-key gate `isCircleNanoEnabled` is **DELETED** with
**zero live consumers** (post-fix census: prod reads of `isCircleNanoEnabled` and `CIRCLE_NANO_API_KEY`
= 0). Prod (both-env-set) behavior is **byte-identical**. The `:2022` dark-gate money boundary, the
all-or-nothing 402 emission, `packages/mcp`, and every (V)-sealed settlement surface are **untouched**.

## Tier — INCREMENTAL (re-confirmed on the realized diff; not escalated)
No escalation trigger fired: the built diff touches **none** of the verifier, the 402 all-or-nothing
emission, any `apps/web/src/lib/settlement/*` file, `packages/mcp`, the `:2022` dark-gate, or any prod
(both-set) visible behavior. `packages/` and `settlement/` stat-diffs are EMPTY; the dark-gate is
absent from the diff (byte-identical, only its line number shifted +7 from the added comments). No
panel reviewer escalated. Integrator's independent pass agrees.

## Executable gate — GREEN (re-run CLEAN + ISOLATED this session; `.audit/b11-seal/gate.log`)
| Gate | Result |
|---|---|
| `tsc --noEmit` | **0** |
| FULL `vitest run` | **4432 passed / 191 files / 0 fail / 0 skip** (baseline 4429 + 3 new) |
| `next build` | **0** (compiled in 31.8s + 746/746 static pages) |
| `next lint` | **0** (warnings are pre-existing, in unrelated files; the 5 touched files are clean) |
| `git diff --numstat` | exactly the 5 files below |
| `packages/` diff · `apps/web/src/lib/settlement/` diff | **EMPTY** · **EMPTY** |

## The review object (5 files; LB-1 = REPLACE)
- `apps/web/src/lib/circle-nano-proxy.ts` (+4/−7): DELETED `isCircleNanoEnabled`; re-keyed the dead
  wrapper arg → `isCircleNanoKernelEnabled`; added the env import.
- `apps/web/src/app/api/proxy/[slug]/route.ts` (+11/−4): enabledMap `:336` + legacy dispatch `:479` →
  `isCircleNanoKernelEnabled`; dropped the now-unused import. Dark-gate `:2022` untouched.
- `apps/web/src/lib/env.ts` (+4/−3): comment-only (FOLD 1) — the stale "still gates on
  `CIRCLE_NANO_API_KEY`" comment rewritten to post-fix reality. No predicate change.
- `apps/web/src/lib/__tests__/proxy-equivalence.test.ts` (+16/−10): mirror `:170` re-keyed in lockstep;
  import swap; `beforeEach` recipient → valid; `:1103` locally stubs recipient='' for discovery-less parity.
- `apps/web/src/app/api/proxy/[slug]/__tests__/circle-nano-proxy-settlement.test.ts` (+63/−7):
  `H.cnanoApiKeySet` toggle; `:309` re-pinned (set/unset → fall-through **401**, FOLD 4 positive); new
  legacy unset/set (→200); new prod-default UNIFIED describe block (set/unset → 401, unset/set → 200)
  pinning `:336` end-to-end.

## The seal-gating panel (`.audit/b11-seal/b11-seal-review.mjs` · run `wf_c396271d-6c4`)
- **Model policy (resolved before fan-out).** Most-capable tier = fable, but **fable INACCESSIBLE this
  session** (probe returned "Claude Fable 5 is currently unavailable") → the sanctioned INCREMENTAL
  fallback = **OPUS** lenses + **OPUS** refuters + opus-session integrator (matches the pre-build panel
  `wf_fe46aa73-9ad`). Effort = session MAX (≥ high). Models set explicitly per agent.
- **Shape.** 4 lens-distinct fresh-context opus reviewers (coverage mode) → per-finding opus refuters
  (default-refuted). Hardened tail (dead-lens guard + refuter-death → fail-safe SUSTAINED). NO
  synthesizer — the integrator (parent session) decided. Lenses: correctness/determinism ·
  spec-conformance+census · core-invariant+funds · test-fidelity+fail-pre-fix (**one above the
  INCREMENTAL floor of 3**, justified by the ~80%-test diff).
- **Result.** 0 dead lenses · 0 refuter-deaths · **10 findings (all LOW) · 0 sustained · 10 refuted.**

### The 10 refuted LOWs + dispositions (every refutation independently audited by the integrator)
| # | Lens | Finding | Disposition |
|---|---|---|---|
| 1 | correctness | mock factory still lists deleted `isCircleNanoEnabled` (`:91`) | Harmless vestige (vitest factories allow extra keys; gate green). P7 hygiene. |
| 2 | correctness | re-key comments cite `:472`/`:332` (pre-edit) vs live `:479`/`:336` | Stale line-number *prose* only; substance correct; matches the chunk doc corpus. Ledger-excluded "trivial doc-imprecision". Not folded (would diverge from trace/plan + re-shift on any edit). |
| 3,8 | spec/core | `mpp.json`/`openapi.json` advertise circle-nano unconditionally | Static catalogs (product capability ≠ instance enablement); pre-existing; **not in the diff**. Settled scope. |
| 4 | spec | fail-pre-fix log captured vs `.not.toBe(503)`, shipped test ships `.toBe(401)` | Targets a non-shipped evidence artifact. Final assertion is provably fail-pre-fix by composition (batch1 ⇒ pre-fix=503; green gate ⇒ post-fix=401). |
| 5 | spec | `proxy-equivalence:274` leaves `CIRCLE_NANO_API_KEY` stubbed (vestigial) | Harmless; hypothetical future re-seed only. P7 hygiene. |
| 6 | core | recipient gate omits the **gas wallet** → recipient-set/gas-unset can advertise an unsettleable 402 | Pre-existing, fail-closed, gate definition untouched; B1.1 conforms to its own bar (recipient = the settlement-capability gate, mirroring `isX402SettlementEnabled`). **Forward note** (relates to banner #2). |
| 7 | core | lost `proxy.circle_nano_not_configured` signal at set/unset | Misconfig-only diagnostic (no money moves either way); deliberately P6's env-preflight seam. |
| 9,10 | test-fidelity | `cnanoApiKeySet` toggle inert post-fix; unset/set unified case corroborates rather than uniquely isolates `:336` | Both self-labeled INFORMATIONAL/no-defect. The fix's proof rests on the fail-pre-fix run + the deletion + code inspection; the set/unset unified case *does* uniquely guard `:336`. |

## Integrator independent verification (this session — corroborates the seal, not just the panel)
- Gate GREEN re-run clean (above).
- Tree-wide census: `isCircleNanoEnabled` = 3 non-prod hits (2 comments + 1 vestigial mock key); `CIRCLE_NANO_API_KEY` = comments + test stubs. **Zero prod consumers/reads** → LB-1=REPLACE proven, env.ts:250 "now unread" comment accurate.
- Published surfaces (`mpp.json:58`, `openapi.json:120`, `learn/protocols`) = static enums, no gate read; `buildMultiProtocol402` + demo/sandbox = no gate read.
- Route structure: kernel gate imported `route.ts:47`; re-keyed `:336`/`:479`; unified path runs first (`:446`) so the unified test pins `:336`; legacy block pins `:479` (`USE_UNIFIED_ADAPTERS='false'`); chain order unperturbed (circle-nano slot 2, before x402).
- Fail-pre-fix empirically real (`.audit/b11-build/batch1-failpre.log`): all 4 matrix cells red pre-fix (503≠401, 401≠200), 7 existing green; post-fix all green.
- Dark-gate (`:2022`) byte-identical (absent from the diff); prod (both-set) byte-invariance holds.

## Forward notes (NOT B1.1 blockers — carried, do not re-litigate inside this chunk)
1. **Gas-wallet coherence axis** (panel finding #6, DC-07-adjacent): the recipient gate is the
   established settlement-capability gate (the sibling `isX402SettlementEnabled` uses the same
   payee-only pattern), but it does not capture **gas-wallet readiness**. Today recipient == the gas
   wallet, so a recipient-set/gas-unset state is largely unreachable; **after banner #2** separates the
   revenue recipient from the gas wallet, ensure the advertise/dispatch gate also reflects gas-wallet
   readiness (else a recipient-set/gas-unset deploy could advertise a 402 it cannot settle — fail-closed
   today). Tie to banner item #2 when it is actioned.
2. **P7-hygiene cluster** (panel #1/#5/#2): the now-inert `H.cnanoApiKeySet` toggle + the mock factory's
   vestigial `isCircleNanoEnabled` key + the vestigial `CIRCLE_NANO_API_KEY` test stub + the
   pre-edit line-number comment references — all harmless, all out of B1.1's minimal scope. Fold into P7.

## The close (founder-gated — DO NOT push; pushes burn the Vercel build limit)
1. **Path-scoped LOCAL commit** (founder):
   `git -C /Users/lex/settlegrid commit -- \`
   `  apps/web/src/lib/circle-nano-proxy.ts \`
   `  "apps/web/src/app/api/proxy/[slug]/route.ts" \`
   `  apps/web/src/lib/env.ts \`
   `  apps/web/src/lib/__tests__/proxy-equivalence.test.ts \`
   `  "apps/web/src/app/api/proxy/[slug]/__tests__/circle-nano-proxy-settlement.test.ts" \`
   `  docs/tech-debt/b1.1-enable-gate-trace-2026-06-13.md \`
   `  docs/tech-debt/b1.1-enable-gate-build-plan-2026-06-13.md \`
   `  docs/tech-debt/b1.1-seal-gate-handoff-2026-06-13.md \`
   `  docs/tech-debt/b1.1-enable-gate-seal-2026-06-13.md \`
   `  docs/tech-debt/b1.1-circle-nano-402-discovery-2026-05-31.md`
   (`.audit/` is gitignored — harness/evidence stays out of the commit. `b1.1-enable-gate-split-handoff-2026-06-12.md` is already in HEAD `98159e04`, so it is NOT re-committed.)
2. **Banner item #1 = CLOSED** (this seal) in `b1.1-circle-nano-402-discovery-2026-05-31.md`.
   **Banner item #2** (move `SETTLEGRID_USDC_RECIPIENT` off the hot gas wallet) **stays a FOUNDER
   checklist item** — NOT this chunk; now also carries forward note #1 above.
3. **Next queue:** P6 ops → P7 hygiene (folds the cluster above) → (G) tidies. **P9 credit-finality =
   founder gate.** Per the cadence, the next chunk re-enters at ① (fresh session).

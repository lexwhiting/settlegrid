{
  "name": "@settlegrid/publish-action",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action to publish MCP tools to SettleGrid registry",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.0.0"
  }
}

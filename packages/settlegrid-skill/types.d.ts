/**
 * @settlegrid/skill is a content-only package (SKILL.md + examples).
 * No runtime exports. This stub exists so tsc's `include: ["*.d.ts"]`
 * resolves at least one file and the editor's language service
 * activates for any future .ts additions.
 */
export {};

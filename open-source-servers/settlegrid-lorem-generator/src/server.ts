import { settlegrid } from "@settlegrid/mcp"
const sg = settlegrid.init({ toolSlug: "lorem-generator", pricing: { defaultCostCents: 1, methods: { generate: { costCents: 1, displayName: "Generate Lorem Ipsum" }, generate_words: { costCents: 1, displayName: "Generate Words" } } } })
const words = "lorem ipsum dolor sit amet consectetur adipiscing elit sed do eiusmod tempor incididunt ut labore et dolore magna aliqua ut enim ad minim veniam quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur excepteur sint occaecat cupidatat non proident sunt in culpa qui officia deserunt mollit anim id est laborum".split(" ")
const generate = sg.wrap(async (args: { paragraphs?: number; sentences_per?: number }) => { const paras = Math.min(args.paragraphs ?? 3, 20); const spp = args.sentences_per ?? 5; const result = Array.from({ length: paras }, () => { const sentences = Array.from({ length: spp }, () => { const len = 8 + Math.floor(Math.random() * 12); const s = Array.from({ length: len }, () => words[Math.floor(Math.random() * words.length)]).join(" "); return s.charAt(0).toUpperCase() + s.slice(1) + "." }); return sentences.join(" ") }); return { paragraphs: result, count: paras, word_count: result.join(" ").split(" ").length } }, { method: "generate" })
const generateWords = sg.wrap(async (args: { count: number }) => { if (!args.count || args.count < 1) throw new Error("count required (1-1000)"); const c = Math.min(args.count, 1000); const result = Array.from({ length: c }, () => words[Math.floor(Math.random() * words.length)]).join(" "); return { text: result, word_count: c } }, { method: "generate_words" })
export { generate, generateWords }
console.log("settlegrid-lorem-generator MCP server ready | 1c/call | Powered by SettleGrid")

import { settlegrid } from "@settlegrid/mcp"
const sg = settlegrid.init({ toolSlug: "barcode-decode", pricing: { defaultCostCents: 1, methods: { identify_format: { costCents: 1, displayName: "Identify Barcode Format" }, validate_ean: { costCents: 1, displayName: "Validate EAN/UPC" } } } })
const identifyFormat = sg.wrap(async (args: { code: string }) => { if (!args.code) throw new Error("code required"); const c = args.code.replace(/[- ]/g, ""); let format = "unknown"; let valid = false; if (/^\d{13}$/.test(c)) { format = "EAN-13"; valid = true } else if (/^\d{12}$/.test(c)) { format = "UPC-A"; valid = true } else if (/^\d{8}$/.test(c)) { format = "EAN-8"; valid = true } else if (/^978\d{10}$/.test(c) || /^979\d{10}$/.test(c)) { format = "ISBN-13"; valid = true } else if (/^[0-9]{14}$/.test(c)) { format = "GTIN-14"; valid = true } else if (/^[A-Z0-9]+$/.test(c) && c.length <= 20) { format = "Code 128 (likely)"; valid = true }; let country = ""; if (format === "EAN-13") { const prefix = c.slice(0, 3); if (prefix >= "000" && prefix <= "019") country = "US/Canada"; else if (prefix >= "300" && prefix <= "379") country = "France"; else if (prefix >= "400" && prefix <= "440") country = "Germany"; else if (prefix >= "450" && prefix <= "459") country = "Japan"; else if (prefix >= "690" && prefix <= "695") country = "China"; else if (prefix >= "880" && prefix <= "880") country = "South Korea" }; return { code: args.code, format, valid, length: c.length, country_prefix: country || undefined } }, { method: "identify_format" })
const validateEan = sg.wrap(async (args: { ean: string }) => { if (!args.ean) throw new Error("ean required (8 or 13 digits)"); const c = args.ean.replace(/[- ]/g, ""); if (!/^\d{8}$|^\d{13}$/.test(c)) throw new Error("Must be 8 or 13 digits"); const digits = c.split("").map(Number); const check = digits.pop()!; let sum = 0; digits.forEach((d, i) => { sum += d * (c.length === 13 ? (i % 2 === 0 ? 1 : 3) : (i % 2 === 0 ? 3 : 1)) }); const expected = (10 - (sum % 10)) % 10; return { ean: args.ean, valid: check === expected, check_digit: check, expected_check_digit: expected, format: c.length === 13 ? "EAN-13" : "EAN-8" } }, { method: "validate_ean" })
export { identifyFormat, validateEan }
console.log("settlegrid-barcode-decode MCP server ready | 1c/call | Powered by SettleGrid")

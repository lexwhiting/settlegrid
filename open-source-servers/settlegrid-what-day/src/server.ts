import { settlegrid } from "@settlegrid/mcp"
const sg = settlegrid.init({ toolSlug: "what-day", pricing: { defaultCostCents: 1, methods: { get_day_info: { costCents: 1, displayName: "Get Day Info" }, days_between: { costCents: 1, displayName: "Days Between Dates" } } } })
const getDayInfo = sg.wrap(async (args: { date?: string }) => { const d = args.date ? new Date(args.date) : new Date(); if (isNaN(d.getTime())) throw new Error("Invalid date"); const days = ["Sunday","Monday","Tuesday","Wednesday","Thursday","Friday","Saturday"]; const months = ["January","February","March","April","May","June","July","August","September","October","November","December"]; const start = new Date(d.getFullYear(), 0, 0); const diff = d.getTime() - start.getTime(); const dayOfYear = Math.floor(diff / 86400000); const isLeap = (d.getFullYear() % 4 === 0 && d.getFullYear() % 100 !== 0) || d.getFullYear() % 400 === 0; const week = Math.ceil(dayOfYear / 7); return { date: d.toISOString().slice(0, 10), day_name: days[d.getDay()], month_name: months[d.getMonth()], day_of_year: dayOfYear, week_of_year: week, is_weekend: d.getDay() === 0 || d.getDay() === 6, is_leap_year: isLeap, quarter: Math.ceil((d.getMonth() + 1) / 3), unix_timestamp: Math.floor(d.getTime() / 1000) } }, { method: "get_day_info" })
const daysBetween = sg.wrap(async (args: { date_a: string; date_b: string }) => { if (!args.date_a || !args.date_b) throw new Error("date_a and date_b required"); const a = new Date(args.date_a); const b = new Date(args.date_b); if (isNaN(a.getTime()) || isNaN(b.getTime())) throw new Error("Invalid date(s)"); const diffMs = Math.abs(b.getTime() - a.getTime()); const days = Math.floor(diffMs / 86400000); return { date_a: args.date_a, date_b: args.date_b, days, weeks: Math.floor(days / 7), months_approx: Math.round(days / 30.44 * 10) / 10, years_approx: Math.round(days / 365.25 * 100) / 100 } }, { method: "days_between" })
export { getDayInfo, daysBetween }
console.log("settlegrid-what-day MCP server ready | 1c/call | Powered by SettleGrid")

{
  "name": "settlegrid-image-classifier",
  "version": "1.0.0",
  "description": "MCP server for image analysis with SettleGrid billing. Classify images, extract metadata, and detect formats from URLs.",
  "type": "module",
  "scripts": {
    "dev": "tsx src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@settlegrid/mcp": "^0.1.1"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "keywords": ["settlegrid", "mcp", "ai", "image-classification", "metadata", "format-detection"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/settlegrid/settlegrid-image-classifier"
  }
}

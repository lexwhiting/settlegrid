import { settlegrid } from "@settlegrid/mcp"
const sg = settlegrid.init({ toolSlug: "postal-rates", pricing: { defaultCostCents: 1, methods: { get_rate: { costCents: 1, displayName: "Get Postal Rate" }, compare_carriers: { costCents: 1, displayName: "Compare Carriers" } } } })
const rates: Record<string, Record<string, { letter_usd: number; package_1kg_usd: number; delivery_days: string }>> = { us: { usps: { letter_usd: 0.68, package_1kg_usd: 8.70, delivery_days: "2-5" }, ups: { letter_usd: 1.50, package_1kg_usd: 12.50, delivery_days: "1-3" }, fedex: { letter_usd: 1.40, package_1kg_usd: 11.80, delivery_days: "1-3" } }, uk: { royal_mail: { letter_usd: 1.10, package_1kg_usd: 5.20, delivery_days: "1-3" }, dhl: { letter_usd: 2.50, package_1kg_usd: 14.30, delivery_days: "1-2" } }, de: { deutsche_post: { letter_usd: 0.95, package_1kg_usd: 6.40, delivery_days: "1-2" }, dhl: { letter_usd: 2.50, package_1kg_usd: 12.80, delivery_days: "1-2" } } }
const getRate = sg.wrap(async (args: { country: string; carrier: string }) => { if (!args.country || !args.carrier) throw new Error("country and carrier required"); const c = rates[args.country.toLowerCase()]; if (!c) throw new Error(`Unknown country. Available: ${Object.keys(rates).join(", ")}`); const r = c[args.carrier.toLowerCase()]; if (!r) throw new Error(`Unknown carrier. Available: ${Object.keys(c).join(", ")}`); return { country: args.country, carrier: args.carrier, ...r } }, { method: "get_rate" })
const compareCarriers = sg.wrap(async (args: { country: string }) => { if (!args.country) throw new Error("country required"); const c = rates[args.country.toLowerCase()]; if (!c) throw new Error(`Unknown. Available: ${Object.keys(rates).join(", ")}`); return { country: args.country, carriers: Object.entries(c).map(([k, v]) => ({ carrier: k, ...v })) } }, { method: "compare_carriers" })
export { getRate, compareCarriers }
console.log("settlegrid-postal-rates MCP server ready | 1c/call | Powered by SettleGrid")

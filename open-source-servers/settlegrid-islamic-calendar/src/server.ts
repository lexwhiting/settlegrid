import { settlegrid } from "@settlegrid/mcp"
const sg = settlegrid.init({ toolSlug: "islamic-calendar", pricing: { defaultCostCents: 1, methods: { convert: { costCents: 1, displayName: "Convert Date" }, get_month: { costCents: 1, displayName: "Get Month Info" } } } })
const months = ["Muharram","Safar","Rabi al-Awwal","Rabi al-Thani","Jumada al-Ula","Jumada al-Thania","Rajab","Sha'ban","Ramadan","Shawwal","Dhu al-Qa'dah","Dhu al-Hijjah"]
const convert = sg.wrap(async (args: { gregorian_date: string }) => { if (!args.gregorian_date) throw new Error("gregorian_date required (YYYY-MM-DD)"); const d = new Date(args.gregorian_date); if (isNaN(d.getTime())) throw new Error("Invalid date"); const jd = Math.floor(d.getTime() / 86400000 + 2440587.5); const l = jd - 1948440 + 10632; const n = Math.floor((l - 1) / 10631); const l2 = l - 10631 * n + 354; const j = Math.floor((10985 - l2) / 5316) * Math.floor((50 * l2) / 17719) + Math.floor(l2 / 5670) * Math.floor((43 * l2) / 15238); const l3 = l2 - Math.floor((30 - j) / 15) * Math.floor((17719 * j) / 50) - Math.floor(j / 16) * Math.floor((15238 * j) / 43) + 29; const m = Math.floor((24 * l3) / 709); const day = l3 - Math.floor((709 * m) / 24); const month = m - 1; const year = 30 * n + j - 30; return { gregorian: args.gregorian_date, hijri: { year, month: month + 1, month_name: months[month] ?? "Unknown", day }, note: "Approximate — actual dates depend on moon sighting" } }, { method: "convert" })
const getMonth = sg.wrap(async (args: { month: number }) => { if (!args.month || args.month < 1 || args.month > 12) throw new Error("month 1-12 required"); const special: Record<number, string> = { 1: "Sacred month, marks Islamic New Year", 3: "Birth of Prophet Muhammad (Mawlid)", 7: "Sacred month, Isra and Mi'raj", 9: "Month of fasting (Ramadan)", 10: "Eid al-Fitr falls on 1st Shawwal", 12: "Sacred month, Hajj and Eid al-Adha" }; return { month: args.month, name: months[args.month - 1], days: args.month % 2 === 1 || args.month === 12 ? 30 : 29, significance: special[args.month] ?? "Regular month" } }, { method: "get_month" })
export { convert, getMonth }
console.log("settlegrid-islamic-calendar MCP server ready | 1c/call | Powered by SettleGrid")

import { settlegrid } from "@settlegrid/mcp"
const sg = settlegrid.init({ toolSlug: "newsletter-format", pricing: { defaultCostCents: 1, methods: { format_newsletter: { costCents: 1, displayName: "Format Newsletter" }, get_template: { costCents: 1, displayName: "Get Template" } } } })
const formatNewsletter = sg.wrap(async (args: { subject: string; sections: { title: string; body: string; cta_text?: string; cta_url?: string }[]; footer?: string }) => { if (!args.subject || !args.sections?.length) throw new Error("subject and sections required"); const html = `<!DOCTYPE html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"></head><body style="font-family:Arial,sans-serif;max-width:600px;margin:0 auto;padding:20px">\n<h1 style="color:#1a1f3a">${args.subject}</h1>\n${args.sections.map(s => `<h2>${s.title}</h2>\n<p>${s.body}</p>\n${s.cta_url ? `<a href="${s.cta_url}" style="background:#10B981;color:white;padding:10px 20px;text-decoration:none;border-radius:4px">${s.cta_text ?? "Learn More"}</a>` : ""}`).join("\n<hr>\n")}\n${args.footer ? `<footer style="color:#666;font-size:12px;margin-top:20px">${args.footer}</footer>` : ""}\n</body></html>`; return { html, subject: args.subject, sections: args.sections.length, estimated_read_min: Math.ceil(args.sections.reduce((a, s) => a + s.body.split(" ").length, 0) / 200) } }, { method: "format_newsletter" })
const templates: Record<string, { name: string; sections: string[]; tone: string }> = { weekly_digest: { name: "Weekly Digest", sections: ["Top Stories", "Industry News", "Tip of the Week", "Upcoming Events"], tone: "informative" }, product_update: { name: "Product Update", sections: ["What's New", "Bug Fixes", "Coming Soon", "Feedback Request"], tone: "enthusiastic" }, welcome: { name: "Welcome Email", sections: ["Welcome Message", "Getting Started", "Key Features", "Support Info"], tone: "warm" } }
const getTemplate = sg.wrap(async (args: { name: string }) => { if (!args.name) throw new Error("name required"); const t = templates[args.name.toLowerCase().replace(/ /g, "_")]; if (!t) throw new Error(`Unknown. Available: ${Object.keys(templates).join(", ")}`); return t }, { method: "get_template" })
export { formatNewsletter, getTemplate }
console.log("settlegrid-newsletter-format MCP server ready | 1c/call | Powered by SettleGrid")

import { settlegrid } from "@settlegrid/mcp"
const sg = settlegrid.init({ toolSlug: "voicemail-api", pricing: { defaultCostCents: 1, methods: { generate_greeting: { costCents: 1, displayName: "Generate Greeting" }, format_transcription: { costCents: 1, displayName: "Format Transcription" } } } })
const generateGreeting = sg.wrap(async (args: { name: string; type?: string; company?: string }) => { if (!args.name) throw new Error("name required"); const type = args.type ?? "professional"; const greetings: Record<string, string> = { professional: `You have reached ${args.name}${args.company ? " at " + args.company : ""}. I am unable to take your call right now. Please leave your name, number, and a brief message, and I will return your call as soon as possible. Thank you.`, casual: `Hey, this is ${args.name}! Sorry I missed your call. Leave a message and I'll get back to you soon!`, after_hours: `Thank you for calling${args.company ? " " + args.company : ""}. Our office hours are Monday through Friday, 9 AM to 5 PM. Please leave a message and we will return your call on the next business day.`, vacation: `Hi, you've reached ${args.name}. I am currently out of the office and will return on [date]. For urgent matters, please contact [alternate]. Otherwise, leave a message.` }; const text = greetings[type]; if (!text) throw new Error(`Unknown type. Available: ${Object.keys(greetings).join(", ")}`); return { greeting: text, type, word_count: text.split(" ").length, estimated_seconds: Math.round(text.split(" ").length / 2.5) } }, { method: "generate_greeting" })
const formatTranscription = sg.wrap(async (args: { text: string; caller?: string; duration_seconds?: number }) => { if (!args.text) throw new Error("text required"); const words = args.text.split(/\s+/).length; return { transcription: args.text, caller: args.caller ?? "Unknown", duration_seconds: args.duration_seconds ?? Math.round(words / 2.5), word_count: words, timestamp: new Date().toISOString() } }, { method: "format_transcription" })
export { generateGreeting, formatTranscription }
console.log("settlegrid-voicemail-api MCP server ready | 1c/call | Powered by SettleGrid")
